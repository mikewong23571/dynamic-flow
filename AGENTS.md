# 项目工作约定

从 [用户故事](docs/user-stories.md) 和 [模块与验收地图](docs/implementation-map.md) 确定本次子问题，再读 [当前设计](docs/design.md)、[Conductor](conductor/index.md) 及所改源码目录的 AGENTS.md。

- 当前做原型：可用、用户友好、实现简单、用户容易理解和指导改动、coding agent 容易修改。
- 安全、权限、隔离及生产级治理不属于原型范围，不据此扩展计划。
- 先复用现成组件，代码集中于核心业务语义；不要先建设通用平台。
- 原型以用户故事能否闭环为取舍依据。职责划分、明确接口、共享类型、错误处理、必要复用与测试是基本工程实践；避免的是无具体需求的通用平台和昂贵抽象。spike/ 是伪代码设计与测试推演，不是已经实现的产品。
- 前端必须遵守统一控件语义和视觉体系，实施前参考真实设计与组件实例，实施后实际操作和检查截图。
- 页面呈现工作内容、状态和动作，避免成片介绍文案；必要标签和错误帮助应清楚。
- 正式应用的实际组件与命令见 conductor/tech-stack.md；历史接入 Spike 见对应 results.md，示例中的实验依赖不自动等于正式产品决定。
- 画布是人的主要编辑入口；画布与 Agent 修改同一份 JS 语义子集 IR，runtime 执行该定义。JS 查看只读，不建设任意 JS 反向分析、源码索引或核心代码编辑器。
- 原设计 v2 原样保存在 docs/archive/，仅供历史参考；不得改写原稿。用户后续明确要求及当前设计优先。
- 旧 Spike 的视觉已被否定；树优先、Monaco 核心编辑器和 AST 待选型等旧建议已被当前画布与 IR 设计取代。
- 保持代码与用户可见概念对应，使用少量明确模块，避免巨大组件和多层通用抽象。

## 实现、实验与验收的区别

正式源码位于 src/，已实现工作区、IR 编辑、运行与比较、Pi 接入、文件保存、模型设置及工作库维护。spike/ 保留伪代码，examples/component-spike/ 是历史接入实验；三者的证据不能混用。默认 `pnpm dev`、`pnpm build`、`pnpm test` 与 `pnpm test:browser` 面向正式应用，旧实验使用 `pnpm spike:*`；`pnpm typecheck:app` 检查正式源码，`pnpm typecheck` 同时检查旧实验与正式源码。实现存在不等于 T1–T8 全部通过，具体结论见当前 track 的验收证据。

模块约定用于拆问题，不是冻结整个解决方案。实现中出现新反例，先记录具体输入、假设失效、受影响故事和最小验证，再同步相关 AGENTS/接口/测试；可以调整边界，不为维护原骨架而删掉业务要求或隐藏失败。

每个模块既要验收自身行为，也要与相邻模块真实联测；最终通过以 T1–T8 用户路线和证据为准。未知清单不穷尽问题，类型检查和 mock 通过不代表完整性。不要为原型预建通用治理体系。

## 文档与基本工程实践

按 [术语与编号](docs/glossary.md) 使用重复、对齐、对比、亲密原则：同类说明结构一致，定义与验收相互对应，区分事实/计划/假设，解释紧邻术语与编号。对读者先写业务名称，再附索引，不能用一串缩写代替说明。

关键模块交接在实现前给出最小输入、输出、错误和具体例子。可修订不等于可以留白；发现假设错误时同步调用双方。普通函数复用、参数传递以便测试、职责分离均可使用，不因“避免抽象”而拒绝合理工程实践。

## 持续业务工作项

[H1–H11 / L1–L5](docs/workitem-stories.md) 是长期工作验收基线。Work 是可复用方法工作空间，WorkItem 是持续业务身份，Run 是固定输入和定义的执行。等待、里程碑、完成条件与重开遵循 [生命周期交接](conductor/tracks/workitem-lifecycle_20260920/handoff.md)；运行成功不等于业务完成。正式交付证据见该 track，旧 T1–T8 仍作为回归。
