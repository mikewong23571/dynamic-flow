# Geist 暗色工作区与节点层级

用户指定参考 Vercel 系列风格、暗色默认、微调现有组件；重做侧边栏列表与画布节点的信息密度。覆盖用户故事 B1/B2、D1、G2 和体验路线 T8。

- 参考 Geist 官方颜色、按钮和暗色实例。采用中性背景/边框/文字层级，白色主操作，蓝色仅表达选中/焦点，状态色各有用途。
- 保留现有 React Flow、Radix、assistant-ui。统一 32/36px 控件、6px 圆角、1px 边框及字体间距，不替换业务栈。
- 侧栏专注工作导航与当前工作资源；最近工作紧凑单行，材料正文和选择进入清楚的输入材料面板，完整文本可读、选中结果持续保存。
- 节点统一图标/类型/名称/状态/端口布局；Agent 明确命名，任务详情只在 Inspector 展示，分支条件保留必要摘要。
- 复查列表、对话、结果、比较、设置、空/错/加载状态的暗色一致性。在1440和1024窗口实际操作并检查截图；已有编辑/运行/重开回归不退化。

不改变 IR、执行语义、LLM 配置或持久化格式，不重建通用主题平台。不把工程验收当作用户审美认可。
