# 模块拆解与验收地图

更新：2026-09-20。目录与入口已创建；所有业务模块仍为 **未实现 / 未验收**。本页服务于把问题拆成可处理的子问题，不宣称覆盖了完整解决方案。

用户目标见 [用户故事](./user-stories.md)，行为推演见 [Spike](../spike/README.md)。源码下的 AGENTS 是后续修改该模块时就近读取的约定；具体接口字段仍可按真实连接调整。

## 1. 模块、职责与子问题

| 模块与就近约定 | 要解决的子问题 | 主要交接 |
| --- | --- | --- |
| [client](../src/client/AGENTS.md) | 用户如何在一个工作区完成 A–F 的任务？ | 后端业务操作、定义快照、节点/工具活动 |
| [server 入口](../src/server/AGENTS.md) | 具体操作、Pi 活动和保存内容怎样成为真实 HTTP/SSE 行为？ | 六个后端功能与前端 |
| [work](../src/server/work/AGENTS.md) | 如何提供材料、拿到产物，并选择正确结果继续？ | files、runs |
| [flow](../src/server/flow/AGENTS.md) | 人与 Agent 如何修改同一份可执行做法？ | files；调用方为 client/assistant/runs |
| [runs](../src/server/runs/AGENTS.md) | 指定做法和输入如何真实执行、停止和重试？ | flow、assistant、files |
| [assistant](../src/server/assistant/AGENTS.md) | Pi 如何生成/修改流程、完成节点任务并返回真实工具活动？ | flow、files、Pi SDK |
| [trials](../src/server/trials/AGENTS.md) | 同输入比较是否成立、是否过期、是否可以采用？ | runs、files；最终判断由用户作出 |
| [files](../src/server/files/AGENTS.md) | 保存后能否重开，失败/中断时状态是否可信？ | Node 文件能力、入口的保存通知 |
| [shared](../src/shared/AGENTS.md) | 调用方是否对同一个字段/状态具有相同理解？ | 只共享实际需要的类型，无业务运行依赖 |

各模块 AGENTS 已包含：职责、目标、非目标、输入输出意图、核心内外依赖、验收用例、联测要求与当前未知。每模块一个目录是为了让约定就近生效，不要求拆包、部署或服务化。

## 2. 如何领取一个子问题

一次实现任务以一条可观察结果为单位，例如“Pi 修改分类要求后画布节点立即更新”，而不是“完成 assistant 全部架构”。明确输入、结果、涉及模块和用例；必要时一个任务可以跨目录完成真实连接。

交付至少说明：改了什么、相关用例的实际结果、与相邻模块的联测证据、尚未验证的假设。涉及接口变更时同步调用方和就近 AGENTS，不让下一个任务按过期假设工作。

源码入口暂不批量生成函数签名；以 [接口意图](../spike/interfaces.md) 为起点，第一条真实调用经过后才固化必要字段。空模块只提供落点，不能当作已交付功能。

## 3. 模块验收的责任

“牵头模块”负责在对应实现任务中给出结果，协作模块不能被永远 mock。完整 Given/When/Then 在 [P01–P31](../spike/tests/cases.md)；这里分配责任，不另写一份重复断言。

| 用例 | 牵头模块 | 联测对象 | 证据重点 | 当前状态 |
| --- | --- | --- | --- | --- |
| P01 | work | client / files | 输入检查与真实创建 | 未实现 / 未验收 |
| P02 | assistant | flow / client | 真实 Pi 生成与失败 | 未实现 / 未验收 |
| P03 | client | runs | 职责节点与运行实例可辨 | 未实现 / 未验收 |
| P04 | flow | runs / client | 连线修改影响实际执行 | 未实现 / 未验收 |
| P05 | flow | client / runs | 局部错误与可修复性 | 未实现 / 未验收 |
| P06 | runs | flow / assistant | 空分支/汇合真实结束 | 未实现 / 未验收 |
| P07 | runs | flow / client | 固定版本与输入 | 未实现 / 未验收 |
| P08 | runs | assistant / server | 真实中止和迟到结果 | 未实现 / 未验收 |
| P09 | runs | files / client | 失败输入局部重试 | 未实现 / 未验收 |
| P10 | client | work / runs | 检查来源与保留选择 | 未实现 / 未验收 |
| P11 | work | runs / client | 报告内容与完整性 | 未实现 / 未验收 |
| P12 | assistant | flow / client | 请求上下文固定 | 未实现 / 未验收 |
| P13 | flow | assistant / client | 旧提案与新修改交错 | 未实现 / 未验收 |
| P14 | trials | runs | 两条输入不扩成全量 | 未实现 / 未验收 |
| P15 | trials | runs / client | 同输入对齐与不兼容提示 | 未实现 / 未验收 |
| P16 | trials | flow / client | 比较过期与迟到完成 | 未实现 / 未验收 |
| P17 | flow | trials / client | 布局与语义分离 | 未实现 / 未验收 |
| P18 | flow | work / files | 采用、保留、放弃独立 | 未实现 / 未验收 |
| P19 | work | runs / client | 混合产物的明确输入 | 未实现 / 未验收 |
| P20 | work | runs / files | 新材料不污染历史 | 未实现 / 未验收 |
| P21 | files | runs / server / client | 真实文件、刷新与重启 | 未实现 / 未验收 |
| P22 | files | flow / client | 写失败不冒充成功 | 未实现 / 未验收 |
| P23 | server | files / client | SSE 重连读取当前状态 | 未实现 / 未验收 |
| P24 | assistant | server / runs | 三协议真实端点分别验证 | 未实现 / 未验收 |
| P25 | client | 产品交付任务 | 浏览器截图及用户视觉判断 | 未实现 / 未验收 |
| P26 | assistant | flow / client | 坏提案和假修改不生效 | 未实现 / 未验收 |
| P27 | trials | runs / client | 停止比较不启动下一侧 | 未实现 / 未验收 |
| P28 | runs | trials / client | 重复启动的实际调用次数 | 未实现 / 未验收 |
| P29 | assistant | flow / files / server / client | 工具实改到 Canvas | 未实现 / 未验收 |
| P30 | runs | assistant / client | 节点/实例/版本活动关联 | 未实现 / 未验收 |
| P31 | server | files / flow / client | 含定义的快照与视口保持 | 未实现 / 未验收 |

shared 的验收嵌入 P07/P14/P16/P19/P21/P30/P31 的真实读写；类型相容不能替代字段含义正确。业务测试放置在 [tests](../tests/AGENTS.md) 的约定下，当前未生成占位测试。

## 4. 模块之间必须连起来验收的路径

| 路线 | 必须实际连接 | 为什么不能只分别验收 |
| --- | --- | --- |
| T1 | client → work/assistant → flow/files → runs → assistant → 结果/报告 | 看得见图、模型能回答、函数能运行，各自成功不等于同一份定义能产出报告 |
| T2 | client → flow → runs → files → client | 拖线成功必须改变真实数据去向；空分支需要真实结束 |
| T3/T4 | Pi 工具 → flow 保存 → 入口快照 → Canvas；trials → 两侧 runs | 工具文字不等于修改；两边结果必须同输入且版本有效 |
| T5 | flow 采用与 work 保留独立 → 明确选择结果 → runs → 新报告 | 不能自动选“最新结果”或把试验产物全当正式输入 |
| T6 | client 停止/重试 → runs → Pi → files → client | 取消 HTTP 成功不等于真正停止；失败重试不能重做全部 |
| T7 | work/files → 真实文件 → 服务重启/页面重连 → client | 内存 mock 无法证明重开与中断状态 |
| T8 | 代表性完整工作区操作 T1/T3/T5 | 单组件截图和自动测试不能替代整体可用性与品味 |

每个实现任务负责自己改变的模块及连接；最终产品交付任务负责 T1–T8。不能在模块全部“完成”之后才第一次尝试这些路径。

## 5. 保持设计与真实问题一致

当前设计足以指导起步，不足以证明完整性。把“已明确的用户目标”和“暂定实现假设”区别对待：目标不因实现困难被偷偷改掉；假设可以被真实证据推翻。

| 暂定假设 | 用什么尽早检验 | 不成立时怎样调整 |
| --- | --- | --- |
| 三协议都可经现有 Pi 调用路径工作 | P24：逐协议工具、流式、取消与错误 | 明确哪种能力不兼容，修正最少接入代码；不假装统一成功 |
| 有限节点/端口足以表达首版任务 | T1/T2/T5 的真实材料和合并输入 | 保留最小无法表达的例子，调整节点或输入规则；不假连线 |
| 同节点比较能承载主要改法 | P15/P19，使用模式变化和多材料结果 | 明确可比/不可比，必要时调整范围；不强行对齐 |
| 工作快照与节点事件足以维护 UI | P29/P30/P31 的迟到、重连与切换版本 | 修正信息和更新顺序，不先建通用事件平台 |
| 单工作文件与顺序执行足够 | 实际材料规模、保存延迟与取消反馈 | 按观测问题调整保存粒度或调度，不提前引入数据库/队列 |
| 画布与上下文面板可让人理解操作 | T8 真实代表页面与用户反馈 | 修改交互/布局和必要模块边界；类型检查不作为反驳反馈的依据 |

这些不是所有未知。出现新问题时，在相关任务/AGENTS 留下四项即可：**具体输入或反例、原假设哪里失败、影响哪些故事/接口、下一步最小验证**。不另建通用风险管理系统。

验收状态区分“未实现、实现中、待验收、通过”；每项通过都要给适用条件和证据。仅 fixture 通过要写 fixture；真实模型或视觉未测则明确留下空缺。已通过项遇到反例应重开，不用文档上的勾选压过事实。

## 6. 下一步顺序

先用一个真实纵向小场景接通“目标/材料 → Pi 生成 → 画布 → 执行 → 报告 → 保存重开”，再扩展画布修改、局部试验、采用与续做。第一条路径就检查模块之间的假设，避免各自实现完整后才发现整体无法衔接。

骨架类型检查与格式检查仅表明入口有效。新运行时、真实模型链路及产品界面仍未实现；当前旧实验的启动/测试命令不能作为这里的验收证据。
