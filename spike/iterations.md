# 伪代码迭代记录

这是设计推演记录，不是执行日志、测试通过报告或运行时证据。所有正式产品路线仍待实现和验收。

## 第 0 轮：写出最直的正常路径

先写工作创建、模型生成、保存草稿、逐节点运行、双边试验、采用和本地保存的伪代码。没有先创建抽象接口、工厂或事件平台。

## 第 1 轮：用异常与交错操作寻找断点

对照 [测试设计](./tests/cases.md)，逐步代入以下条件，初稿不能给出正确答案：

| 用例 | 初稿断点 | 必须补上的最小行为 |
| --- | --- | --- |
| P13、P26 | saveDraft 不检查请求起点，旧模型提案可能覆盖用户新修改 | expectedDraftId 比对；所选节点范围检查；过期提案只提示，不自动合并 |
| P16、P17 | readComparison 没有判断当前候选与样本是否变化 | 用固定定义 ID 与输入值判断有效性；坐标和列表排序不参与 |
| P08、P09 | stop 立即标取消、execute 未检查迟到结果；实例失败后的去向不明确 | stopping → abort → cancelled；调用前后检查；失败实例记录且阻止依赖汇总 |
| P19 | 旧新结果直接打包，可能重复选择同一材料 | 显式输入预览与同材料冲突提示；不取隐式“最新结果” |
| P21、P22、P23 | 直接覆盖文件、重开没有中断判断；事件掉线无法重建页面 | 文件完成后再更新引用；写成功才确认；启动整理中断状态；SSE 首次/重连发送完整状态 |
| P15、P27、P28 | 比较两次 start 没有统一停止、输入契约检查或重入处理 | 比较固定输入并顺序运行；停止不启动第二侧；同工作只允许一条活跃运行链 |

## 第 2 轮：修正后重新沿用户路径推演

已在 flow/assistant 中加入草稿起点检查，在 trials 中区分执行状态与比较有效性，在 runs 中补调用前后停止检查与失败去向，在 work 中补续做输入预览/确认，在 files 中补保存失败和重开规则。没有为此增加通用抽象模块。

重新沿 [T1–T8](./tests/walkthroughs.md) 逐步推演：每步都有负责函数、固定输入、结果保存与界面反馈。推演还发现两处接口描述缺口并同步修正：续做需要明确“预览/确认”参数；比较有效性检查需要当前基线 ID，不能只接收当前样本。

## 第 3 轮：交付完整性检查

已实际用本地脚本检查：20 份 Markdown、27 个 Spike 内的本地文档链接有效；每个目录都有 README；19 个故事全部进入覆盖矩阵；28 个用例编号唯一且均被路线说明引用；T1–T8 完整；所有实现片段为明确标注的 `.pseudo.md`，未生成可执行应用；原始归档 SHA-256 不变。另核对接口表中的 22 个具名函数引用，都有对应伪代码说明；新增入口链接有效。这些只是结构与覆盖检查，不证明用例行为通过。

仍需实现后验证：三种协议 SDK 映射及真实流式/工具/取消；模型输出质量；异步保存/停止交错；真实文件重启与故障；代表性界面的视觉与交互。这些没有被文档推演消除。
