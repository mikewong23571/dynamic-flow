# 正式应用浏览器域内回归

2026-09-20，当前工作树（未提交）。命令 `pnpm test:browser`，3/3 通过，13.0 秒。真实前端 4320、真实 HTTP/SSE 后端 4321；本组只运行已有 identity / branch / merge 函数流程，没有调用模型。

- `workspace.spec.ts`：1440×900 与 1024×768 各自新建独立自动验收工作。真实点击节点改名、保存条件、添加重复来源连接；保存局部错误后运行被拒绝且没有新 Run。删除坏连接后完整运行成功；分流恰好 1 条匹配、2 条不匹配，汇合仍为原始三条材料。结果与来源可读，刷新后配置、版本和同一运行记录保持，未产生重复运行。检查页面宽度及主要控件边界，无横向溢出、无 pageerror。
- `work-library.spec.ts`：HTTP 创建 12 项独立工作；浏览器搜索并切换 10/20 条分页、重命名（同步最近工作）、归档/恢复、重新打开并刷新。服务端 titleEdited=true，原目标和材料未改变。列表在两种尺寸实际查看截图，标题、目标、动作与分页可读。

截图来自最后通过的一轮，并人工查看列表两尺寸：

- [1440 工作列表](work-library-1440.png)
- [1024 工作列表](work-library-1024.png)
- [1440 运行结果](results-1440.png)
- [1024 运行结果](results-1024.png)
- [1440 重开](reopened-1440.png)
- [1024 重开](reopened-1024.png)

首次调通时修正测试定位：原生 select 使用 combobox 角色；新增工作列表后同名入口要限定最近工作导航。一次 1440 刷新撞到开发期间 API 重启而取得空响应，保持重开断言；稳定 API 上最后一轮三项全部通过。

## 测试工作保留方式

根任务要求回归结束后归档所有测试数据。已通过现有 archive API 归档 33 项 goal 以“自动验收”或“前端域内检查”开头的工作；没有删除材料、定义、结果。清单和 ID 见 [fixtures.json](fixtures.json)。真实客户反馈工作 `7f617b1f-ead0-400f-958b-3f4560a3ce5f` 未修改，清理后仍为活动工作。

可在“所有工作 → 已归档”中搜索并恢复，或调用 `POST /api/works/:id/actions`，正文 `{"action":"archive","archived":false}`。回归测试每次都建新的独立工作；重跑后产生的新 fixture 可按同样规则归档。

本组证明正式模块在函数 fixture 下的 UI/HTTP/持久化闭环，不代替根任务的真实 Pi、T1–T8 完整用户路线或用户视觉接受。

最终根任务复验追加 same-work-reopen.spec.ts，4/4 通过（16.5秒）：同工作重开、保留未提交输入、放弃后刷新、当前工作归档移出最近及恢复重现。最新回归数据另外归档，见当前 track 的 final-checks.json。
